# 假面骑士吧长帖审阅助手隐私说明

生效日期：2026-08-17

适用版本：1.0.0

本扩展是非百度、非贴吧及相关作品权利方官方的人工审阅辅助工具。本说明描述扩展会处理什么数据、数据保存在哪里、何时会发送给第三方 AI 服务商，以及用户如何删除这些数据。

## 1. 处理的数据

当用户在贴吧帖子中打开侧栏并开始整帖读取时，扩展会处理当前帖子中接口可见的：

- 帖子地址、帖子编号和标题；
- 主楼、楼层及楼中楼的文字、公开用户名、楼层关系、回复关系和公开时间；
- 图片节点数量、分页状态、读取进度及接口完整性诊断；
- 用户在扩展内作出的审核决定、选择的规范理由和相关回复编号。

贴吧同域只读请求由 Chrome 按网页规则携带当前贴吧登录会话。扩展不申请 `cookies` 权限，不直接读取、保存或导出 Cookie 值、密码及贴吧登录凭据。

## 2. 本机处理与保存

扩展使用当前 Chrome 配置文件的扩展存储保存以下信息：

- 主题、服务商、模型、自动读取及自动分析等设置；
- 用户自行提供的 AI API 密钥。密钥按服务商分开保存，不进入普通设置、分析历史或导出文件；
- 成功的 AI 分析历史，包括规范化帖子地址/编号、帖子标题、模型生成的摘要、线索、报告、引用回复标识与楼层位置、模型配置和服务商返回的 token 用量；
- 独立的调用用量台账，包括记录时间、服务商、模型和服务商回执的输入/输出/合计 token 数；
- 人工审核决定及其与分析结果的关联；
- 为防止扩展更新、关闭、数据损坏或删除报告后自动重复付费而保存的最小任务/计费回执。回执只保存哈希标识、尝试 ID、状态和时间；
- 运行期间的读取快照、任务状态和错误分类。包含原始正文与用户名的读取快照只写入内存型 `chrome.storage.session`，会在相关标签关闭或导航离开该帖时清理，并在浏览器重启、扩展更新/重载或禁用时清空。

历史的结构化字段不单独保存原始回复正文、公开用户名或证据摘录；但模型生成的摘要和报告可能概括或复述帖子内容。历史最多保存 100 次，并与单条限制共同受 4 MiB 总预算约束；扩展不会静默淘汰旧报告。

扩展存储不是操作系统密钥库级加密。扩展会把 `chrome.storage.local` 限制为可信扩展上下文，但能访问当前 Chrome 配置文件或扩展调试环境的人仍可能取得其中数据。请只在受信任的设备和 Chrome 配置文件中使用。

## 3. 发送给第三方 AI 的数据

免费整帖读取不会调用 AI。只有用户点击“开始 AI 初筛”，或用户明确开启自动付费分析后，扩展才会向当前选择的 AI 服务商发送请求。

发送前，扩展会把作者替换为本次请求内的代号，把回复标识替换为临时代号，并脱敏常见手机号、邮箱、身份证、QQ 和微信信息。请求包含脱敏后的帖子标题、接口可读的文字、真实楼层号、相对首条回复的分钟偏移、父子/互动关系、图片数量等必要覆盖说明和本地规范全文。

请求不包含：

- 帖子 URL、贴吧官方 PID/SPID 和精确发言时间；
- 原始用户名、图片文件、Cookie 值、密码或贴吧登录信息；
- 其他标签页或其他网站的内容。

支持的第三方服务商为用户所选的阿里云百炼工作区或 DeepSeek。请求直接从扩展发送到对应 API 域名，本项目不运营转发服务器。服务商可能按其自身条款记录、保留或处理请求；费用、账单和服务商侧数据删除由用户与相应服务商处理。人民币费用以服务商后台为准。

## 4. 数据用途与共享

数据仅用于：读取当前帖子、验证读取范围、生成待人工复核的 AI 线索、定位原帖证据、保存用户选择和防止未授权的重复模型请求。

本扩展不投放广告，不出售数据，不建立跨网站用户画像，不把数据提供给数据经纪商，也不包含独立遥测或分析 SDK。除用户选择的 AI 服务商以及贴吧自身的只读请求外，扩展不会主动把帖子内容发送到其他服务器。

## 5. 权限用途

- `https://tieba.baidu.com/*`：读取当前贴吧帖子并在原页面内定位证据；不调用发帖、删帖、封禁等写入端点。
- 阿里云百炼固定工作区域名：在用户选择该服务商时发送脱敏后的 AI 请求。
- `https://api.deepseek.com/*`（可选）：经用户授权后发送脱敏后的 DeepSeek 请求。
- `http://localhost/*` 与 `http://127.0.0.1/*`（可选）：开发/本地兼容端点；正式用户无需授权即可正常使用官方服务商。
- `storage`：保存设置、密钥、分析历史、人工决定及最小任务回执。
- `sidePanel`：提供侧栏界面。
- `scripting`：在当前贴吧帖子中注入只读采集与证据定位脚本。

## 6. 保留、导出与删除

- AI 分析报告可在扩展的“历史”页逐项删除。人工决定目前没有逐项删除入口；要清除它们，需要清除该扩展的全部本机数据或卸载扩展。
- token 用量记录与最小防重复计费回执目前也没有独立删除入口；它们不会随报告删除，清除全部扩展数据或卸载扩展会一并移除。
- API 密钥可在设置中按服务商单独清除；卸载扩展、更换 Chrome 配置文件或清除扩展数据也会删除密钥。
- 删除一份 AI 报告不会自动删除关联的人工决定、token 用量记录或最小防重复计费回执，界面会在删除前说明这一点。
- 用户可以分别导出/导入 AI 历史和人工决定。导出的文件由用户自行选择保存位置，离开扩展后由用户负责保管和删除。
- 若要彻底清除全部本机扩展数据，可在 `chrome://extensions` 中移除本扩展，或通过 Chrome 的扩展存储清理功能清除该扩展数据。

本版本没有专门的无痕模式存储隔离。请不要在无痕窗口启用本扩展；若用户手动允许扩展在无痕模式运行，相关设置和历史仍可能写入该 Chrome 配置文件的普通扩展存储。

## 7. 自动化决定与人工复核

AI 输出只是待人工复核的辅助线索。扩展不会自动删除、封禁、发帖或执行任何贴吧管理操作，也不会把模型判断当作最终裁决。所有线索都必须由用户返回原帖核对。

## 8. 变更与联系

若数据处理方式发生实质变化，本文件会更新生效日期与适用版本。问题与反馈可提交至：<https://github.com/Morphling0717/Tieba/issues>。
