# 版本记录

本项目遵循语义化版本号。1.0.0 是首个冻结功能范围、通过正式构建验证的 Chrome 版本。

## 1.0.0 — 2026-08-17

### 主要功能

- 严格读取贴吧长帖主回复和楼中楼，校验分页、稳定回复编号、重复节点、未知结构与覆盖范围。
- 采用“处理清单优先”的 AI 初筛结果页，展示真实证据摘录、完整规范原文、待确认信息和页内原帖定位。
- 免费自动读取与可能付费的 AI 分析完全分离；付费默认手动，最多两项并行且不设置暗中队列。
- 切换浏览器标签不会取消已发送任务或串帖；Snapshot 级计费回执防止更新、断连或删除报告后自动重复付费。
- 引入精简的 V3 结构化报告协议，并兼容旧版报告的只读折叠展示。
- API 密钥按服务商保存在当前 Chrome 配置文件，更新、重载与 Chrome 重启后仍可使用。
- 成功的 AI 分析历史、token 回执与关联人工决定均在本机持久化；AI 历史支持独立导入、导出和删除报告，人工决定支持独立导入、导出。
- 理由库按 16 类、112 条规范原文就地展开和分组搜索。
- 普通/夜间主题、窄屏、键盘、高对比与 reduced-motion 适配；加入克制的页面、按钮、选择、展开和主题动效。

### 安全与边界

- 只使用贴吧只读端点，不执行发帖、删除、封禁或其他官方管理操作。
- 发送 AI 前替换作者和回复标识并脱敏常见个人信息；不发送帖子 URL、官方 PID/SPID、精确时间、图片或 Cookie 值。
- 读取不完整或输出截断会整份拒绝；未知回复 ID 不生成可点击目标，未知规则 ID 不会成为公开理由，其余合法结果仍可保留。
- 证据定位留在当前页面，失败时不会自动刷新或跳转官方深链。

### 已知限制

- 正式实机验收平台为 Chrome 140 及以上；Edge 与 Tabbit 仅保留兼容实现。
- 不识别图片、视频或表情图中的文字。
- 贴吧非公开只读端点可能改版、限流或只返回当前账号可见内容。
- 必须由人工复核模型对反讽、引用、玩笑、事实真伪和上下文的判断。
- 成功分析历史最多 100 次，总预算 4 MiB，仅保存在当前 Chrome 配置文件且不同步。
- 关闭侧栏可能使已发送请求进入“结果未知”；扩展不会因此自动重发。
